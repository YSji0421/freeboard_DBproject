<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/nav.css">
</head>
<body>
    <%- include('../partials/nav', {user: user}) %>
    
    <div class="container">
        <h1><%= title %></h1>
        
        <form action="/board/<%= post ? 'edit/' + post.post_id : 'write' %>" method="POST">
            
            <% if (user.email === 'admin@example.com') { %>
                <div class="form-group">
                    <label>분류</label>
                    <select name="type" style="padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                        <option value="free" <%= (post && post.type === 'free') ? 'selected' : '' %>>일반글</option>
                        <option value="notice" <%= (post && post.type === 'notice') ? 'selected' : '' %>>공지사항</option>
                    </select>
                </div>
            <% } else { %>
                <input type="hidden" name="type" value="free">
            <% } %>

            <div class="form-group">
                <label>제목</label>
                <input type="text" name="title" required 
                       style="width: 100%; padding: 10px;"
                       value="<%= post ? post.title : '' %>">
            </div>

            <div class="form-group" style="margin-top: 15px;">
                <label>내용</label>
                <textarea name="content" required 
                          style="width: 100%; height: 300px; padding: 10px;"
                          ><%= post ? post.content : '' %></textarea>
            </div>

            <div style="margin-top: 20px; text-align: right;">
                <button type="submit" class="button">저장</button>
                <a href="javascript:history.back()" class="button" style="background: #6c757d;">취소</a>
            </div>
        </form>
    </div>
</body>
</html>